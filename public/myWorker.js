onmessage = function (e) {
    const { numbers } = e.data;
    for (let index = 0;; index++) {
        // const element = array[index];
        console.log("hello");
    }
    // postMessage(sum);
  };